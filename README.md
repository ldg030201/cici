# cici — Claude In Chrome Id

Find the `bridgeDeviceId` of the **Claude in Chrome** extension for every Chrome profile on your machine — the ID Claude Code asks you to pick when more than one browser is connected — without opening the extension's service worker DevTools and running `chrome.storage.local.get(null, ...)` by hand.

_Work in progress._
